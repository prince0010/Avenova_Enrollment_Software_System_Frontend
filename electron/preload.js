// Intentionally empty: the renderer is the Next.js app itself and doesn't
// need any Node/Electron APIs exposed yet. Add contextBridge.exposeInMainWorld
// calls here if a future feature needs the renderer to reach native APIs.
